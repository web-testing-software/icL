#ifndef VM_ERRORS_H
#define VM_ERRORS_H

namespace vm {

enum class Error {
	COMMAND_NOT_FOUND,
	COMMAND_EXECUTION_ERROR,
	INVALID_ARGUMENTS,
	UNEXPECTED_EOF,
	DATA_CONVERSION_ERROR,
	WRONG_STACK_STATE,
	SERVER_ERROR,
	FILE_NOT_OPENED,
	WRONG_FILE_FORMAT,
	NO_VM_ERROR
};

}

#endif // VM_ERRORS_H
